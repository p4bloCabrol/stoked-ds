# Package registry
registry=https://registry.npmjs.org/

# Enforce exact versions when saving
save-exact=true

# Always generate and respect lockfile
package-lock=true

# Audit: fail on high or critical severity
audit-level=high
